* /var/www/
    nginx root, index.html is here

* /var/www/pub/
    public shared files, can be accessed through http

* /var/www/pub/socks1314.pac
    updated hourly

* /var/www/pub/config.txt
    user rules for socks1314.pac, add domains to it if is blocked

* /var/pub/
    public shared files between users, not available for outside world
